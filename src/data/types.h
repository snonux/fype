#ifndef TYPES_H
#define TYPES_H

typedef enum {
   TYPE_UNKNOWN,
   TYPE_NUMBER,
   TYPE_STRING,
   TYPE_VOIDP,
   TYPE_SYMVAR,
   TYPE_VARIABLE,
   TYPE_REGEXPR,
   TYPE_OPERATOR,
   TYPE_STACK,
   TYPE_TUPEL,
   TYPE_HASH,
   TYPE_DAT,
   TYPE_ARG_DAT,
   TYPE_ARGS_DAT,
   TYPE_STATEMENT_DAT,
   TYPE_CODE_DAT
} TYPE;

typedef enum {
   RET_OK,
   RET_ERROR = -1,
   RET_NO_SPACE = -2,
   RET_OCCUPIED = -3
} RETCODE;

#endif
